/**
 * Button component translations
 * Exports all available translations for the Button component
 */

import { sv } from './sv';
import { en } from './en';
import type { TranslationDictionary, ComponentTranslation } from '../../../../core/schemas/i18n/types';

export const buttonTranslations: TranslationDictionary<ComponentTranslation> = {
  sv,
  en,
};

export { sv, en };
