import {
  Body,
  Heading,
  Typography,
  Label
} from './Typography';

export const typographyComponents: Record<string, React.ComponentType<any>> = {
  body: Body,
  heading: Heading,
  label: Label,
  typography: Typography,
};