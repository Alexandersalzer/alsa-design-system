import {
  Body,
  Heading,
  Typography,
} from './Typography';

export const typographyComponents: Record<string, React.ComponentType<any>> = {
  body: Body,
  heading: Heading,
  typography: Typography,
};