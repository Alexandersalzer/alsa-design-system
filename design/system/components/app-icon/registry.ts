import AppIcon from './AppIcon/AppIcon';
import AppIconGrid from './AppIconGrid/AppIconGrid';

export const appIconComponents: Record<string, React.ComponentType<any>> = {
  appIcon: AppIcon,
  appIconGrid: AppIconGrid,
};